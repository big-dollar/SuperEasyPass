# 系统托盘功能说明

## 新增功能

### 1. 系统托盘图标
- 程序启动后会在系统托盘（右下角）显示一个图标
- 图标使用 `RuxiPass.ico` 文件
- 鼠标悬停在图标上会显示提示："超容易密码管理器 (SuperEasyPass)"

### 2. 关闭窗口行为
- **之前**：点击关闭按钮会直接退出程序
- **现在**：点击关闭按钮会将窗口最小化到系统托盘，程序继续在后台运行
- 首次最小化到托盘时，会显示一个气泡提示，告知用户程序已最小化到托盘

### 3. 重新打开窗口
有两种方式可以重新打开主窗口：
- **双击托盘图标**（推荐）
- 右键点击托盘图标，选择"显示主窗口"

### 4. 托盘菜单
右键点击托盘图标会显示菜单：
- **显示主窗口**：打开主界面
- **退出程序**：真正退出应用程序

### 5. 完全退出程序
要完全退出程序，有两种方式：
- 右键点击托盘图标，选择"退出程序"
- 在主窗口打开的情况下，通过托盘菜单选择"退出程序"

## 使用场景

1. **快速隐藏**：当需要快速隐藏密码管理器时，直接点击关闭按钮即可
2. **后台运行**：程序可以一直在后台运行，需要时双击托盘图标即可快速打开
3. **配合快捷键**：结合全局快捷键功能，可以更方便地使用密码管理器

## 技术实现

- 使用 PyQt5 的 `QSystemTrayIcon` 实现系统托盘功能
- 重写 `closeEvent` 方法，将关闭事件改为隐藏窗口
- 托盘图标支持双击激活和右键菜单
- 程序退出时会自动清理托盘图标

## 注意事项

- 如果 `RuxiPass.ico` 文件不存在，程序会使用系统默认图标
- 程序会在 `main.py` 中设置 `app.setQuitOnLastWindowClosed(False)`，确保关闭窗口后应用继续运行
