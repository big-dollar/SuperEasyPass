# 一键复制功能说明

## 功能概述

在右侧密码详情区域，您可以通过**单击**用户名或密码字段，快速将内容复制到剪贴板。

## 使用方法

### 1. 查看密码详情
- 在左侧密码列表中点击任意一条记录
- 右侧会显示该记录的用户名和密码（密码以星号显示）

### 2. 复制用户名
- **单击**右侧"用户名"字段
- 用户名会自动复制到剪贴板
- 系统托盘会弹出气泡提示："用户名已复制到剪贴板！"

### 3. 复制密码
- **单击**右侧"密码"字段（星号显示的那一行）
- 真实密码会自动复制到剪贴板
- 系统托盘会弹出气泡提示："密码已复制到剪贴板！"

## 功能特点

✅ **一键操作**：无需手动选择和复制，点击即可
✅ **安全隐私**：密码始终以星号显示，只在复制时获取真实值
✅ **无打断体验**：使用托盘气泡提示，不会弹出对话框打断您的操作
✅ **快速反馈**：1.5秒的气泡提示，既能确认操作成功，又不会长时间占用屏幕

## 使用场景

1. **快速登录**：点击密码复制后，直接切换到登录页面粘贴
2. **批量操作**：需要多次使用同一账号时，可以快速复制用户名和密码
3. **配合快捷键**：结合全局快捷键 `Ctrl + 右键` 自动填充功能，实现更高效的密码管理

## 技术实现

- 使用 PyQt5 的 `QApplication.clipboard()` 实现剪贴板操作
- 通过 `cellClicked` 信号监听表格单元格点击事件
- 使用 `QSystemTrayIcon.showMessage()` 显示轻量级提示
- 在内存中临时保存当前查看的用户名和密码，用于复制操作
